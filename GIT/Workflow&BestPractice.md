1. best practice

using clear commit messages that are meaningfull

git commit -m “Add login form with validation”

git commit -m “stuff” → not recommended

workflow habbits

keep the commits small and forcused

always pull before pushing to avoid conflicts

uses branches for features, and not experimenting directly to the main

1. Usefull commands
- git slash → templory saves uncommited changes without commiting them
- git fetch → downlaods the changes from the remote without merging them yet
- git remote → check or manages the remote repos

1. What to expect

imagin collaborating with a group of developers from the student-poratal project